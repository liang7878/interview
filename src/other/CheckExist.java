package other;

public class CheckExist {
    public boolean Find(int target, int [][] array) {
        boolean result = false;
        int x_size = array.length;
        if(x_size == 0) {
            return false;
        }

        int y_size = array[0].length;
        if(y_size == 0) {
            return false;
        }


        return result;
    }
}
